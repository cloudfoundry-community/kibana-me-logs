define(['settings'],
function (Settings) {
  return new Settings({
    elasticsearch: "{{ .Url }}",
    default_route: '/dashboard/file/app-logs.json',
    kibana_index: "kibana-int",
    panel_names: [
      'histogram',
      'map',
      'goal',
      'table',
      'filtering',
      'timepicker',
      'text',
      'hits',
      'column',
      'trends',
      'bettermap',
      'query',
      'terms',
      'stats',
      'sparklines'
    ]
  });
});
